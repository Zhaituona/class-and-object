public class DoWhileLoop {
    public static void main(String[] args) {


        int count = 0;//this do while loop is first can run the do body , the  check the while condition. so
        //the loop can run at least one time , this will show 1 to 10 //
        do {
            System.out.println(count);
            count++;
        } while (count <= 10);
    }
   // int count = 15;//this do while loop is first can run the do body , the  check the while condition. so
    //the loop can run at least one time, this one we can run and check , it will show 15  //
       // do {
        //System.out.println(count);
       // count++;
  //  } while (count <= 10);//
}
